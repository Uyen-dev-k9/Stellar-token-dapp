[package]
name = "token_vault"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "token_vault"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }